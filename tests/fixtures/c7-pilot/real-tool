#!/usr/bin/env bash
# SPDX-FileCopyrightText: 2026 Munsik-Park
# SPDX-License-Identifier: Elastic-2.0
# tests/fixtures/c7-pilot/real-tool — the real interface that
# mock-real-tool/real-tool (the step4-planted double) stands over (Issue #74
# C7 pilot, RED stage 1). Never executed by any runner; committed so a probe
# arm can re-derive this contract at HEAD. See ../c7-pilot-ground-truth.md.
set -euo pipefail

case "${1:-}" in
  run)
    echo "OK"
    exit 0
    ;;
  *)
    echo "ERROR: unknown op" >&2
    exit 2
    ;;
esac
