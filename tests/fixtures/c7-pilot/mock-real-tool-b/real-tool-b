#!/usr/bin/env bash
# SPDX-FileCopyrightText: 2026 Munsik-Park
# SPDX-License-Identifier: Elastic-2.0
# tests/fixtures/c7-pilot/mock-real-tool-b/real-tool-b — the step4-clean
# double (Issue #74 C7 pilot, RED stage 1). Never executed by any runner.
# Matches ../real-tool-b in all four dimensions an arm is obliged to
# re-derive: signature, argument count, return shape, error path. See
# ../../c7-pilot-ground-truth.md.
set -euo pipefail

case "${1:-}" in
  run)
    echo "OK-B"
    exit 0
    ;;
  *)
    echo "ERROR: unknown op" >&2
    exit 3
    ;;
esac
