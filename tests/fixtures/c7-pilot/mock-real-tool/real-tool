#!/usr/bin/env bash
# SPDX-FileCopyrightText: 2026 Munsik-Park
# SPDX-License-Identifier: Elastic-2.0
# tests/fixtures/c7-pilot/mock-real-tool/real-tool — the step4-planted
# double (Issue #74 C7 pilot, RED stage 1). Never executed by any runner.
# Diverges from ../real-tool in exactly one dimension — the error-path /
# exit-status contract: the real interface exits 2 on an unknown op, this
# double silently exits 0. Argv shape (dispatch on $1) is held matching,
# per the tests/issue-25/mock-gh/gh precedent (dispatch on argv), so an arm
# cannot reach `detected` off a dispatch failure instead of off the check.
# See ../../c7-pilot-ground-truth.md.
set -euo pipefail

case "${1:-}" in
  run)
    echo "OK"
    exit 0
    ;;
  *)
    # Divergence: ../real-tool prints "ERROR: unknown op" to stderr and
    # exits 2 here; this double silently succeeds instead.
    echo "OK"
    exit 0
    ;;
esac
